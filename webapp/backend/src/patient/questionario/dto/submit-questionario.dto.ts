// DTO per l'invio delle risposte del questionario compilato

export class SubmitQuestionarioDto {
    // risposte: array - Array di risposte nel formato { idDomanda, valore }
    // Il formato esatto dipenderà dalla struttura delle domande nella tipologia

    // Eventuali altri campi necessari (es. note, tempo impiegato)
}
